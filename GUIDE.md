# 🚀 VPN за 5 минут — Краткая инструкция

Эта инструкция для тех, кто ценит время. Все сложные шаги автоматизированы.

## Этап 1: Подготовка сервера
Арендуйте любой VPS за рубежом (Ubuntu 22.04/24.04). Получите IP и пароль root.

## Этап 2: Установка (одна команда)
1. Подключитесь по SSH и загрузите файлы проекта в `/root/VPN/`.
2. Выполните:
```bash
cd /root/VPN && chmod +x master_setup.sh && ./master_setup.sh
```

## Этап 3: Настройка клиента
1. Выполните `python3 scripts/bot/vpn_manager.py --show-clients` на сервере. Или просто нажмите кнопку "Ссылки" в Telegram-боте.
2. Скопируйте ссылку VLESS или Hysteria2 в приложение (Hiddify, Streisand).
3. **Готово!**

---

## 🆘 Помощь при проблемах

### 1. Не могу войти по SSH
- Если вы уже запустили установку, порт изменился на **2222**.
- Используйте: `ssh -p 2222 root@IP`.

### 2. Панель 3x-ui не открывается
- Проверьте, запущен ли контейнер: `docker ps`.
- Проверьте фаервол: `ufw status`.

### 3. VPN подключен, но нет интернета
- Проверьте порт 443 (TCP/UDP).
- Выполните `docker restart hysteria2 3x-ui`.

### 4. Нужен бэкап или перенос
- Нажмите кнопку "Бекап" в вашем Telegram-боте.

### 5. Ошибка "No space left on device"
- Docker накопил мусор. Выполните: `docker system prune -a`.

### 6. Не работают сайты (ошибка DNS)
- Если интернет есть, но сайты не открываются, выполните:
  `echo "nameserver 8.8.8.8" > /etc/resolv.conf`

### 7. Ошибка при запуске master_setup
- Проверьте логи: в конце вывода скрипт всегда пишет точный номер строки с ошибкой. Попробуйте исправить `.env` и запустить снова.

---

## ⚙️ Коротко: где лежат настройки?
- **Все секреты и IP:** в файле `.env`. Если что-то там поменяли — запустите `./master_setup.sh` снова.
- **Список клиентов:** используйте Telegram-бота или команду `python3 scripts/bot/vpn_manager.py --show-clients`.
- **Лимиты скорости:** в `.env` (параметры `HYSTERIA_UP` и `HYSTERIA_DOWN`).
- **Сайт, под который маскируемся:** в `.env` (`REALITY_SNI`). Если сменили — не забудьте обновить его и в настройках Inbound в панели 3x-ui.

---

## 💡 Полезные команды
- `python3 scripts/bot/vpn_manager.py --show-clients` — Показать ссылки для подключения.
- Telegram-бот позволяет сменить порты панели и Hysteria2 в один клик.
- `docker logs -f 3x-ui` — Посмотреть, что происходит с VPN.

---

## 🗑️ Как полностью удалить VPN
Если вы хотите переустановить систему или очистить сервер:
1. Выполните:
   ```bash
   chmod +x uninstall.sh && ./uninstall.sh
   ```
2. Скрипт остановит контейнеры, очистит порты 443/80 и удалит временные данные.
