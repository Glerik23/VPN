# 🚀 VPN за 15 минут — Пошаговая инструкция для новичков

> Эта инструкция написана для тех, кто **никогда не работал с серверами**. Каждый шаг подробно объяснён. Просто следуй инструкции сверху вниз.

---

## 📖 Что мы будем делать?

Мы арендуем сервер за рубежом и установим на него VPN. После этого ты сможешь подключиться к нему с телефона или компьютера и пользоваться интернетом без ограничений.

**Что получишь в итоге:**
- 🔒 Свой личный VPN, который никто не заблокирует
- 📱 Подключение с телефона (iOS/Android) и компьютера
- ⚡ Высокая скорость (зависит от сервера)
- 👥 Можно подключить семью и друзей

**Стоимость:** ~300-500/мес за аренду сервера (хватит на 5-10 человек)

---

## 📝 Словарь терминов

| Термин | Простое объяснение |
|--------|-------------------|
| **VPS** | Виртуальный сервер — как удалённый компьютер, который работает 24/7 |
| **SSH** | Способ подключиться к серверу через командную строку (как пульт управления) |
| **SSH-ключ** | Цифровой "паспорт" для входа на сервер (безопаснее пароля) |
| **Терминал** | Программа для ввода текстовых команд |
| **Docker** | Программа, которая запускает приложения в изолированных "коробках" |
| **VLESS** | Протокол VPN — способ шифрования трафика |
| **REALITY** | Технология маскировки — делает VPN-трафик неотличимым от обычного |

---

## Этап 1: Арендуй VPS-сервер

### 1.1. Выбери хостинг

Зайди на сайт любого хостинг-провайдера, который предлагает VPS за рубежом. Популярные варианты легко найти через поиск: «VPS аренда за рубежом».

### 1.2. Выбери параметры сервера

При заказе выбери:
- **ОС (операционная система):** Ubuntu 22.04 или Ubuntu 24.04
- **Тариф:** самый дешёвый (1 ядро, 512MB-1GB RAM, 10GB SSD) — этого хватит
- **Расположение:** любая доступная страна

### 1.3. Получи данные для входа

После оплаты тебе придут данные:
```
IP-адрес:   123.45.67.89      ← адрес твоего сервера
Логин:      root               ← имя пользователя
Пароль:     какой-то-пароль    ← пароль для входа
```

> 📌 Сохрани эти данные — они понадобятся дальше.

---

## Этап 2: Подключись к серверу

### На Windows

1. **Скачай программу MobaXterm** (бесплатная):
   - Зайди на сайт [mobaxterm.mobatek.net](https://mobaxterm.mobatek.net)
   - Нажми **«Download»** → **«Home Edition»** → **«Installer edition»**
   - Установи и открой

2. **Подключись к серверу:**
   - Нажми **«Session»** (вверху слева) → **«SSH»**
   - **Remote host:** впиши IP-адрес сервера (например `123.45.67.89`)
   - **Username:** впиши `root`
   - Нажми **«OK»**
   - Введи пароль (символы НЕ отображаются при вводе — это нормально)
   - Нажми Enter

### На macOS

1. Открой программу **Терминал** (найди через Spotlight: ⌘+Пробел → набери «Terminal»)
2. Введи команду:
   ```bash
   ssh root@123.45.67.89
   ```
   (замени `123.45.67.89` на IP твоего сервера)
3. Набери `yes` и нажми Enter (если спросит)
4. Введи пароль и нажми Enter

### Если всё правильно

Ты увидишь что-то вроде:
```
root@server:~#
```
**Поздравляю, ты на сервере!** 🎉 Теперь можно вводить команды.

---

## Этап 3: Загрузи файлы VPN на сервер

### 3.1. Скачай этот проект

Скачай все файлы из этой папки на свой компьютер (если ещё не скачал).

### 3.2. Загрузи файлы на сервер

**В MobaXterm (Windows):**
- Слева есть файловый менеджер сервера
- Перетащи всю папку `VPN` в `/root/` на сервере

**Или через командную строку** (на своём компьютере, не на сервере):
```bash
scp -r ./VPN/* root@123.45.67.89:/root/vpn/
```
(замени IP на свой)

### 3.3. Вариант Б: Через GitHub (Рекомендуемый)

Если ты планируешь обновлять VPN, лучше всего загрузить файлы на GitHub.

**На компьютере (Windows):**
1. [Создай токен на GitHub](https://github.com/settings/tokens) (права: `repo`).
2. В папке проекта:
   ```powershell
   git init
   git add .
   git commit -m "vpn setup"
   git remote add origin https://github.com/Glerik23/VPN.git
   git push -u origin main
   ```
   *Когда спросит пароль — вставь Токен.*

**На сервере:**
```bash
git clone https://github.com/Glerik23/VPN.git /root/vpn
cd /root/vpn
```

### 3.4. Перейди в папку на сервере


---

## Этап 4: Настрой конфигурацию

### 4.1. Создай файл настроек

```bash
cp .env.example .env
```

### 4.2. Открой файл для редактирования

```bash
nano .env
```

### 4.3. Заполни значения

Используй стрелки на клавиатуре для навигации. Измени эти строки:

```
SERVER_IP=123.45.67.89              ← замени на IP твоего сервера
XUI_PASSWORD=MyStrongPassword123    ← придумай сложный пароль
```

> ⚠️ Остальное **не трогай** — скрипты заполнят автоматически.

### 4.4. Сохрани и выйди

1. Нажми **Ctrl+O** (сохранить)
2. Нажми **Enter** (подтвердить имя файла)
3. Нажми **Ctrl+X** (выйти из редактора)

---

## Этап 5: Запусти установку

### 5.1. Сделай скрипты исполняемыми

```bash
chmod +x scripts/*.sh
```

### 5.2. Защити сервер

```bash
./scripts/01-init-server.sh
```

⏳ Подожди 1-2 минуты. Скрипт:
- Настроит безопасный вход по SSH
- Включит фаервол
- Установит защиту от взлома
- Оптимизирует сеть

> ⚠️ **ВАЖНО!** После этого скрипта SSH-порт изменится на **2222**.
> Открой **НОВЫЙ** терминал (не закрывай старый!) и проверь, что можешь подключиться:
> ```bash
> ssh -p 2222 root@123.45.67.89
> ```
> Если подключился — всё ок, продолжай. Если нет — НЕ ЗАКРЫВАЙ старый терминал и обратись за помощью.

### 5.3. Установи Docker

```bash
./scripts/02-install-docker.sh
```

⏳ Подожди 2-3 минуты. Скрипт установит Docker — программу для запуска VPN.

### 5.4. Запусти VPN

```bash
./scripts/03-deploy.sh
```

⏳ Подожди 1-2 минуты. Скрипт:
- Сгенерирует все ключи шифрования
- Настроит VPN-конфигурацию
- Запустит VPN-сервер

В конце ты увидишь:
```
  📊 Панель 3x-ui:
     URL:      http://123.45.67.89:2053
     Логин:    admin
     Пароль:   MyStrongPassword123
```

---

## Этап 6: Настрой VPN в панели управления

### 6.1. Открой панель

Открой в браузере (Chrome, Firefox и т.д.) ссылку из предыдущего шага:
```
http://123.45.67.89:2053
```
(замени IP на свой)

### 6.2. Войди в панель

- **Username:** `admin`
- **Password:** пароль, который ты указал в `.env`

### 6.3. Создай VPN-подключение

1. В меню слева нажми **«Inbounds»** (Входящие подключения)
2. Нажми **«+ Add Inbound»** (Добавить)
3. Заполни поля:

| Поле | Что вписать |
|------|-------------|
| **Remark** | Любое название, например `My VPN` |
| **Protocol** | Выбери **VLESS** |
| **Port** | `443` |
| **Transmission** | **tcp** |
| **Security** | **reality** |
| **uTLS** | **chrome** |
| **Dest** | `www.microsoft.com:443` |
| **Server Names** | `www.microsoft.com` |

4. Нажми кнопку **«Get new cert»** или вручную впиши ключи из `.env`:

```bash
# На сервере выполни, чтобы увидеть ключи:
cat /root/vpn/.env
```

Найди и впиши:
| Поле в панели | Значение из .env |
|---------------|------------------|
| **Private Key** | `REALITY_PRIVATE_KEY=...` |
| **Short Ids** | `REALITY_SHORT_ID=...` |
| **ID (UUID)** в секции Client | `VLESS_UUID=...` |
| **Flow** в секции Client | `xtls-rprx-vision` |

5. Нажми **«Create»** (Создать)

---

## Этап 7: Подключись с телефона или компьютера

### 7.1. Получи ссылку

На сервере выполни:
```bash
./scripts/05-show-clients.sh
```

Ты увидишь длинную ссылку вида:
```
vless://abc123...@123.45.67.89:443?...#VPN-VLESS-REALITY
```

И QR-код в терминале.

### 7.2. Установи VPN-приложение

#### 📱 iPhone / iPad
1. Открой **App Store**
2. Найди и установи **«Streisand»** (бесплатно) или **«Hiddify»**
3. Открой приложение
4. Нажми **«+»** → **«Добавить из буфера обмена»**
5. Или отсканируй QR-код с экрана терминала

#### 🤖 Android
1. Открой **Google Play**
2. Найди и установи **«Hiddify Next»** или **«v2rayNG»**
3. Открой приложение
4. Нажми **«+»** → **«Импорт из буфера обмена»**
5. Или отсканируй QR-код

#### 🖥️ Windows
1. Скачай **Hiddify Next** с [GitHub](https://github.com/hiddify/hiddify-next/releases)
2. Установи и открой
3. Нажми **«+»** → **«Добавить из буфера обмена»**
4. Вставь скопированную ссылку

#### 🍎 macOS
1. Скачай **Hiddify Next** с [GitHub](https://github.com/hiddify/hiddify-next/releases)
2. Или установи **Streisand** из App Store
3. Добавь ссылку или отсканируй QR-код

### 7.3. Подключись!

В приложении нажми кнопку **подключения** (обычно большая круглая кнопка).

Если статус стал **«Подключено»** — **готово, VPN работает!** 🎉

---

## ❓ Частые вопросы

### Как добавить друга/семью?
В панели 3x-ui зайди в свой Inbound → нажми **«+»** в секции **Client** → создай нового клиента с новым UUID. Каждому человеку — свой UUID.

### Как проверить, что VPN работает?
Подключись к VPN и зайди на [2ip.io](https://2ip.io) — ты должен увидеть IP своего VPN-сервера, а не свой домашний.

### Интернет стал медленный через VPN?
- Это зависит от расстояния до сервера и его пропускной способности
- Попробуй сменить протокол на Hysteria2 (используй вторую ссылку из `05-show-clients.sh`)

### VPN перестал работать?
1. Зайди на сервер по SSH
2. Проверь, работают ли контейнеры:
   ```bash
   cd /root/vpn
   docker compose ps
   ```
3. Если контейнеры не запущены:
   ```bash
   docker compose up -d
   ```

### Как обновить VPN?
```bash
cd /root/vpn
docker compose pull
docker compose up -d
```

### Как сделать бэкап?
```bash
./scripts/06-backup.sh
```
Архив сохранится в `/root/vpn-backups/`.

### Забыл пароль от панели 3x-ui?
```bash
docker exec -it 3x-ui /app/x-ui setting -username admin -password новый_пароль
```

---

## 🆘 Что-то пошло не так?

| Проблема | Что делать |
|----------|------------|
| Не могу подключиться к серверу по SSH | Попробуй через VPS-консоль на сайте хостинга |
| Панель 3x-ui не открывается в браузере | Проверь, правильный ли IP и порт (2053). На сервере выполни: `docker logs 3x-ui` |
| VPN подключается, но интернет не работает | Проверь, что порт 443/TCP открыт: `ufw status` |
| QR-код не сканируется | Скопируй текстовую ссылку вручную |
| Скрипт выдаёт ошибку | Скопируй текст ошибки и поищи в интернете |

---

> 💡 **Совет по безопасности:** после успешной настройки измени порт панели 3x-ui на нестандартный. Это делается одной командой:
> ```bash
> ./scripts/07-secure-panel.sh
> ```
> Скрипт спросит новый порт, автоматически обновит фаервол, fail2ban и перезапустит панель.
