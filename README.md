# 🛡️ VPN Server — Полный деплой-пакет

Готовый к продакшену VPN-сервер с **VLESS + REALITY** и **Hysteria 2**.
Оптимизирован для обхода DPI (ТСПУ) и возможных белых списков.

## 📋 Почему этот стек?

| Протокол | Зачем | Против чего работает |
|----------|-------|----------------------|
| **VLESS + REALITY** | Основной. Маскируется под TLS-соединение к реальному сайту. DPI видит обычный HTTPS. | ТСПУ, DPI, белые списки (трафик выглядит как обычный HTTPS) |
| **Hysteria 2** | Резервный. UDP/QUIC протокол с высокой скоростью и masquerade. | QoS, throttling, нестабильные каналы |

> **Против белых списков:** REALITY не требует собственного сертификата — он "заимствует" TLS-отпечаток реального сайта (SNI). Даже при белых списках трафик выглядит как обычное соединение к крупному сайту.

---

## 🖥️ Требования

- **VPS за рубежом**
  - Ubuntu 22.04+ или Debian 12+
  - Минимум 1 vCPU, 512MB RAM, 10GB SSD
- **SSH-ключ** (рекомендуется; пароли будут автоматически отключены только при наличии ключей)
- **Чистый IP** (проверь: `ping IP`, [check-host.net](https://check-host.net))

---

## 🚀 Быстрый старт

### 1. Подготовка (на твоём компьютере)

```bash
# Перенеси файлы на сервер
scp -r ./* root@YOUR_SERVER_IP:/root/vpn/
```

### 2. На сервере

```bash
ssh root@YOUR_SERVER_IP
cd /root/vpn

# Создай .env из шаблона
cp .env.example .env
nano .env  # ← заполни SERVER_IP и XUI_PASSWORD

# Сделай скрипты исполняемыми
chmod +x scripts/*.sh

# 1️⃣ Защити сервер (SSH, UFW, fail2ban, BBR)
./scripts/01-init-server.sh

# ⚠️ ВАЖНО: проверь SSH в НОВОМ терминале перед продолжением!
# ssh -p 2222 root@YOUR_SERVER_IP

# 2️⃣ Установи Docker
./scripts/02-install-docker.sh

# 3️⃣ Запусти VPN (автогенерация ключей + настройка 3x-ui + docker up)
./scripts/03-deploy.sh

# 4️⃣ Получи ссылки для клиентов
./scripts/05-show-clients.sh
```

4. Сохрани

> 💡 **Совет:** Теперь панель можно настроить **автоматически** прямо из терминала! Просто выбери "y", когда `03-deploy.sh` спросит об этом, или запусти отдельно: `./scripts/08-setup-inbound.sh`.

> 💡 **Безопасность:** После настройки запусти `./scripts/07-secure-panel.sh`, чтобы сменить порт панели на нестандартный. Скрипт обновит фаервол и fail2ban автоматически.

---

## 📱 Клиентские приложения

### iOS
| Приложение | VLESS | Hysteria2 | Цена |
|------------|-------|-----------|------|
| **Streisand** | ✅ | ✅ | Бесплатно |
| **Shadowrocket** | ✅ | ✅ | $2.99 |
| **Hiddify** | ✅ | ✅ | Бесплатно |

### Android
| Приложение | VLESS | Hysteria2 | Цена |
|------------|-------|-----------|------|
| **Hiddify Next** | ✅ | ✅ | Бесплатно |
| **v2rayNG** | ✅ | ❌ | Бесплатно |
| **NekoBox** | ✅ | ✅ | Бесплатно |

### Windows / macOS / Linux
| Приложение | VLESS | Hysteria2 | Платформы |
|------------|-------|-----------|-----------|
| **Hiddify Next** | ✅ | ✅ | Win / Mac / Linux |
| **Nekoray** | ✅ | ✅ | Win / Linux |
| **v2rayN** | ✅ | ❌ | Windows |

### Как подключиться
1. Установи приложение из списка выше
2. Запусти `./scripts/05-show-clients.sh` на сервере
3. Скопируй ссылку или отсканируй QR-код
4. Добавь как новый профиль → Подключись

---

## 🛠️ Администрирование

### Проверка состояния
```bash
# Статус контейнеров
docker compose ps

# Логи 3x-ui
docker logs -f 3x-ui --tail 50

# Логи Hysteria2
docker logs -f hysteria2 --tail 50

# Статус fail2ban
fail2ban-client status sshd
```

### Обновление
```bash
cd /root/vpn
docker compose pull
docker compose up -d
```

### Бэкап
```bash
./scripts/06-backup.sh
# Архив: /root/vpn-backups/vpn-backup-YYYYMMDD_HHMMSS.tar.gz
```

### Смена SNI (если текущий заблокирован)
1. Найди новый сайт: `curl -I https://www.samsung.com` (должен быть 200)
2. Измени `REALITY_SNI` и `REALITY_SERVER_NAME` в `.env`
3. Обнови в 3x-ui: Inbound → Edit → Dest + Server Names
4. Обнови в клиенте: изменить SNI

**Хорошие варианты SNI:**
- `www.microsoft.com`
- `www.google.com`
- `dl.google.com`
- `www.samsung.com`
- `www.logitech.com`
- `www.amd.com`

---

## 🗂️ Структура проекта

```
VPN/
├── .env.example            # Шаблон конфигурации
├── .env                    # ⛔ Твои секреты (не коммитить!)
├── .gitignore              # Защита от утечки секретов
├── docker-compose.yml      # Docker stack
├── README.md               # Эта инструкция
├── hysteria2/
│   ├── config.yaml.template # Шаблон конфига Hysteria2
│   ├── config.yaml         # Рабочий конфиг (создаётся автоматически)
│   └── cert/               # ⛔ TLS сертификаты
├── scripts/
│   ├── 01-init-server.sh   # Защита сервера
│   ├── 02-install-docker.sh# Установка Docker
│   ├── 03-deploy.sh        # Главный деплой
│   ├── 04-generate-keys.sh # Генерация ключей
│   ├── 05-show-clients.sh  # Ссылки клиентов
│   ├── 06-backup.sh        # Бэкап
│   ├── 07-secure-panel.sh  # Смена порта панели
│   └── 08-setup-inbound.sh # Авто-настройка 3x-ui
└── configs/
    └── fail2ban/
        ├── jail.local      # Правила банов
        └── filter.d/
            └── 3x-ui.conf  # Фильтр для панели
```

---

## ⚠️ Безопасность

- 🔑 **SSH:** автоматическое отключение паролей при наличии ключей, нестандартный порт, защита от случайного lockout.
- 🧱 **UFW:** открыты только необходимые порты
- 🚫 **fail2ban:** автобан после 3 неудачных попыток
- 🔄 **unattended-upgrades:** автоматические патчи безопасности
- 🚀 **BBR:** TCP congestion control для лучшей скорости
- 📦 **Docker:** изоляция сервисов

---

## 🐛 Troubleshooting

| Проблема | Решение |
|----------|---------|
| Не подключается VLESS | Проверь порт 443/TCP: `ufw status`, `docker compose ps` |
| Не подключается Hysteria2 | Проверь порт 443/UDP: `ufw status`, `ss -unlp \| grep 443` |
| 3x-ui панель не открывается | `docker logs 3x-ui`, проверь порт XUI_PORT в UFW |
| SSH не работает после hardening | Если вы не добавили ключ, вход по паролю останется активным. Проверьте порт (2222). |
| IP заблокирован (ТСПУ) | Смени SNI в `.env` и 3x-ui, или попробуй Hysteria2 |
| Медленная скорость | Включи BBR: `sysctl net.ipv4.tcp_congestion_control` должен показать `bbr` |
