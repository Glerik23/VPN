# 🛡️ VPN Server — Полный деплой-пакет

Готовый к продакшену VPN-сервер с **VLESS + REALITY** и **Hysteria 2**.
Оптимизирован для обхода DPI (ТСПУ) и блокировок.

## Быстрый старт

### 1. Подготовка (на твоём компьютере)
```bash
# Перенеси файлы на сервер
scp -r ./* root@YOUR_SERVER_IP:/root/VPN/
```

### 2. На сервере
```bash
ssh root@YOUR_SERVER_IP
cd /root/VPN

# Сделай скрипт исполняемым и запусти установку
chmod +x master_setup.sh
./master_setup.sh
```
> 💡 Скрипт сам настроит SSH, установит Docker, сгенерирует ключи и запустит VPN. Вам нужно только заполнить **SERVER_IP** и **XUI_PASSWORD** в `.env`, когда скрипт об этом попросит.

---

## 📱 Клиентские приложения
- **iOS:** Streisand, Hiddify, Shadowrocket
- **Android:** Hiddify Next, v2rayNG, NekoBox
- **PC:** Hiddify Next, Nekoray

**Как подключиться:** запустите `./scripts/05-show-clients.sh` на сервере для получения ссылок и QR-кодов.

---

## 🛠️ Администрирование
```bash
docker compose ps       # Статус контейнеров
docker logs -f 3x-ui    # Логи панели
./scripts/06-backup.sh  # Бэкап настроек
```

---

## � Решение проблем (Troubleshooting)

| Проблема | Решение |
|----------|---------|
| Не подключается VLESS | Проверь порт 443/TCP: `ufw status` |
| Не подключается Hysteria2 | Проверь порт 443/UDP: `ss -unlp | grep 443` |
| Панель не открывается | Проверь порт панели: `docker logs 3x-ui` |
| SSH не работает | По умолчанию порт сменён на **2222**. Проверьте это при входе. |
| Медленная скорость | BBR включен автоматически, проверьте канал сервера. |

---

## 📖 Шпаргалка: где и что менять?

| Если нужно изменить... | Где искать | Что сделать после |
|-------------------------|------------|-------------------|
| **Пароли/Ключи** | `.env` | Перезапуск: `./master_setup.sh` |
| **Порт SSH (2222)** | `.env` → `SSH_PORT` | Перезапуск + обновить UFW |
| **Сайт-маскировку (SNI)**| `.env` → `REALITY_SNI` | Обновить в панели 3x-ui |
| **Скорость Hysteria2** | `.env` → `HYSTERIA_UP/DOWN` | `docker restart hysteria2` |
| **Порт панели 3x-ui** | Скрипт `./scripts/07-secure-panel.sh` | Скрипт сделает всё сам |
| **DNS (блокировка рекламы)** | Панель AdGuard (порт 3000) | Настройки в браузере сервера |

---

## 🗂️ Структура проекта
- `master_setup.sh` — 🚀 Главный скрипт установки
- `scripts/` — Вспомогательные скрипты (бекап, клиенты, защита)
- `hysteria2/` — Конфигурация Hysteria2
- `.env` — Ваши секреты и настройки
